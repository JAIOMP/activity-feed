import { ActivityFeed } from '../ActivityFeed/ActivityFeed';
import { Layout } from '../Layout/Layout';

export const Home = () => {
  return (
    <Layout>
      <ActivityFeed />
    </Layout>
  );
};
